#include "Preprocessing.h"

Mat filterLanes(Mat img) {
	// Mat hsvImage;
	// cvtColor(img, hsvImage, COLOR_BGR2HSV);

	// Mat yellowHueRange;
	// Mat whiteHueRange;
	// inRange(hsvImage, Scalar(20, 100, 100), Scalar(30, 255, 255), yellowHueRange);
	// inRange(hsvImage, Scalar(200, 200, 200), Scalar(255, 255, 255), whiteHueRange);

	return img;
}

Mat applyGaussianBlur(Mat img) {
	return img;
}

Mat applyCannyEdgeDetection(Mat img) {
	return img;
}
